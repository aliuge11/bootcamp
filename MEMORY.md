# MEMORY.md

Bu dosya [agentmemory](https://github.com/rohitg00/agentmemory)'nin yaklaşımından ilham alıyor, kendisini değil. agentmemory'nin sunucusu, hook'ları, veritabanı, vektör/BM25/graph arama motoru burada yok — kurulmuyor. Alınan şey, onun hafızayı katmanlara ayırma disiplini: ham gözlem → oturum özeti → kalıcı gerçek → tekrar eden iş akışı. Burada bu katmanlar otomatik bir motorla değil, Claude Code'un (ve sizin) bu dosyayı oturum sonlarında elle güncellemesiyle işliyor.

## Nasıl çalışıyor

| Katman | agentmemory'de | Burada |
|---|---|---|
| Working | Ham tool-call gözlemi, hook'larla otomatik | O anki konuşmanın kendisi — buraya yazılmıyor, kalıcı değil |
| Episodic | LLM ile sıkıştırılmış oturum özeti | Aşağıdaki "Oturum Özetleri" — önemli bir karar/değişiklik olduğunda elle eklenen kısa, tarihli satır |
| Semantic | Çıkarılmış gerçekler + kavramlar | Aşağıdaki "Bilinen Gerçekler" — INTENT/CLAUDE/DESIGN/STYLE'ta zaten yazılı olmayan, ama unutulursa yeniden keşfedilmesi gerekecek küçük gerçekler |
| Procedural | Öğrenilmiş iş akışı kalıpları | Aşağıdaki "Bu Projede Nasıl Çalışıyoruz" — bu projede tekrar eden, CLAUDE.md'nin genel ilkelerinden daha spesifik alışkanlıklar |

Kaynak izlenebilirliği (provenance) agentmemory'de otomatik; burada elle — her Semantic/Procedural madde, geldiği karara veya belgeye referans veriyor.

## Oturum Özetleri

- **2026-07-19 — INTENT.md:** [docs/intent/etkilesimli-sehir-haritasi.md](docs/intent/etkilesimli-sehir-haritasi.md) onaylandı. PowerPoint gömme yöntemi Web Viewer eklentisi olarak netleşti, en fazla 4 harita + senkron şehir seçimi, xlsx yükleme periyodik ve kalıcı, xlsx şeması (Mali no / İl / İlçe / SLA durum) ve kargo sayısı/başarı oranı formülleri belirlendi.
- **2026-07-19 — CLAUDE.md:** Karpathy'den türetilmiş davranış ilkeleri (düşün, sadeleştir, cerrahi değişiklik, hedef odaklı yürüt) proje-özel mimari kısıtlarla tek dosyada birleştirildi.
- **2026-07-19 — DESIGN.md:** BMW'nin kurumsal sitesinden (designmd.co) renk ilhamı, [google-labs-code/design.md](https://github.com/google-labs-code/design.md) formatında yazıldı. Sonra harita renklendirmesi ödeal'in "SLA Dışı Kuralanlar" referans görseline göre 5 kategorili, kesin eşikli bir skalaya (kırmızı/turuncu/mavi/yeşil/gri) revize edildi; il sınırları genel görünümde, ilçe sınırları sadece drill-down'da çizgili olacak şekilde netleşti; dış ok/bağlantı-çizgili etiketleme yerine il/ilçe adları doğrudan haritanın üzerine yazılacak.
- **2026-07-19 — STYLE.md:** [stop-slop](https://github.com/hardikpandya/stop-slop) kuralları Türkçeye ve "sade, kurumsal" iç araç tonuna uyarlandı.
- **2026-07-19 — PLAN.md:** [agentmemory](https://github.com/rohitg00/agentmemory)'nin katmanlı hafıza yaklaşımı MEMORY.md'ye (kurmadan, sadece disiplin olarak) uyarlandıktan sonra, sıfırdan koda geçiş için 10 adımlık uçtan uca bir yapım planı yazıldı. Bu adımda kritik bir teknik karar da netleşti: Türkiye il/ilçe GeoJSON verisi için [coskunomer/Turkish-Cities-Geojson-Dataset](https://github.com/coskunomer/Turkish-Cities-Geojson-Dataset) (MIT) seçildi ve doğrulandı; DB erişimi için ORM yerine ham `pg`, stil için Tailwind (DESIGN.md token'larından) kararlaştırıldı. PLAN.md, kullanıcının "hiçbir şey sorma" talimatına uyacak şekilde her teknik boşluğu (routing şeması, xlsx okuma kuralı, senkron seçim + drill-down davranışı) baştan sabitliyor.
- **2026-07-19 — PLAN.md Adım 1-10 (MVP tamamlandı):** Proje sıfırdan tek oturumda kodlandı ve doğrulandı. Next.js (App Router, TS) + Tailwind v4 + native PostgreSQL üzerine: GeoJSON tabanlı il/ilçe haritası (d3-geo, react-simple-maps değil), xlsx içe aktarma + aggregate hesaplama, 5 kategorili SLA renklendirmesi + lejant, il→ilçe drill-down, şehir detay paneli, en fazla 4 haritalı senkron karşılaştırma (`/map/compare`), yükleme geçmişi + ana sayfa, iframe gömme doğrulaması. Her adım kendi commit'i, kendi tarayıcı doğrulaması ve `npm test`/`npm run build` kontrolüyle ilerledi (21 Vitest testi, tüm build'ler temiz). Yol boyunca ortaya çıkan teknik gerçekler ve kararlar aşağıdaki "Bilinen Gerçekler"de. Ayrıntılı adım adım geçmiş için `git log` (18+ commit) yeterli — bu özet onun yerini tutuyor.
- **2026-07-19 — MVP sonrası kullanıcı geri bildirimi:** Kullanıcı gerçek ekran görüntüsüyle 4 düzeltme istedi: (1) koyu tema, (2) sayfa içi Geri/İleri navigasyonu, (3) detay panelinin ilçeleri örtüp tıklanmaz kılması, (4) ilçe etiketlerinin taşması. (3) ve (4) DESIGN.md'nin kendi "haritayı örtmüyor" kuralına aykırı bir uygulama hatasıydı (absolute overlay), gerçek karardan sapma değildi. Yapılanlar: DESIGN.md'nin renk tokenları koyu temaya çevrildi (SLA 5 rengi aynen korundu, sadece arayüz kromu değişti), `useSelectionHistory` hook'uyla sayfa-içi geri/ileri yığını + `NavToolbar` eklendi (tarayıcı geçmişinden bağımsız, çünkü iframe'de tarayıcı çerçevesi yok), `CityDetailPanel` absolute overlay'den flex sütununa çevrildi (harita artık gerçekten daralıyor, hiçbir ilçeyi kapatmıyor), SVG'lere `overflow-hidden` eklendi. Karşılaştırma görünümünde (çoklu harita) panel genişliği 320px→256px düşürüldü çünkü her haritanın kendi paneli açılabiliyor ve alan kısıtlı. Hepsi tarayıcıda (tek harita + 2'li karşılaştırma, 1280px) doğrulandı: panel/harita çakışmıyor, etiket taşması yok, Geri/İleri doğru çalışıyor, ✕ paneli kapatıp drill-down durumunu koruyor.
- **2026-07-19 — Silme özelliği:** Kullanıcı yüklemeleri silebilmek istedi — bu INTENT.md'nin "kalıcı URL, hiç silinmiyor" kararıyla çelişiyordu (PowerPoint'e gömülü linkler bozulmasın diye). Kullanıcıya sorup **soft-delete**'te (listeden gizle, veri/link kalıcı kalsın) karar kılındı. `uploads.hidden` kolonu eklendi (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, ayrı migration aracı yok — schema.sql tekrar çalıştırılabilir kalsın diye). Sadece **liste** sorguları (`/api/uploads` GET, ana sayfa) `WHERE hidden = false` filtreliyor; `id`'ye göre doğrudan sorgular (`/map/[id]`, `/map/compare`, `/api/uploads/[id]` GET) filtrelemiyor — hidden bir yükleme direkt linkle hâlâ açılıyor. `DELETE /api/uploads/[id]` satırı silmiyor, `hidden=true` set ediyor. Tarayıcıda uçtan uca doğrulandı: silinen öğe listeden kayboluyor, doğrudan `/map/[id]` linki hâlâ açılıyor.
- **2026-07-19 — Liste ekranı görsel cila:** Kullanıcının ekran görüntüsü üzerinden 3 nokta atışı düzeltme: native checkbox `accent-primary` ile temaya uyduruldu, "N satır" `text-metric-md` (tabular-nums) ile sağa hizalandı, "Sil" `muted-soft`'a çekilip ikincil aksiyon gibi görünmesi sağlandı (hover'da `muted`'a açılıyor). Dropzone/boşluk düzeni zaten DESIGN.md token'larına uyduğu için dokunulmadı.
- **2026-07-19 — 3-4 haritalı karşılaştırma 2 sütunlu ızgara:** Önce sadece 4 harita için 2x2'ye geçildi (1280px'te ~295px/harita → 608px/harita). Kullanıcı hemen ardından 3 haritanın da aynı mantıkla (2 üstte + 1 altta) bölünmesini istedi — `gridColumns`/`borderClass` genelleştirildi: `count <= 2 ? count : 2` sütun, kenarlık hesaplama artık "sağımda/altımda gerçekten bir harita var mı" sorusuna göre (index aritmetiği), sabit 2x2 varsayımı değil. Bu sayede 3 haritada yalnız kalan alt öğe gereksiz sağ kenarlık almıyor. 1-2 harita hâlâ tek sıra. Tarayıcıda 1/2/3/4 harita için kenarlık ve senkron tıklama regresyonsuz doğrulandı.
- **2026-07-19 — Otomatik karşılaştırma özeti:** Kullanıcı "iki harita arasındaki karşılaştırmayı sen yapacaksın" dedi — ilk ve son yükleme arasında somut, net bir yorum cümlesi (başarı oranı eski→yeni + puan farkı + yön, kargo sayısı eski→yeni) istendi; şehre tıklayınca şehir bazlı, ilçeye tıklayınca ilçe bazlı olacak. `src/lib/comparison.ts` (saf fonksiyonlar: aggregateRegionStats, basariOrani, compareTotals — 9 Vitest testiyle) + `ComparisonSummary.tsx` eklendi. Özellik, `/map/compare`'in mevcut senkron seçim mekanizmasına (`MapPanel`'in `selected` prop'u) oturtuldu — herhangi bir 2 harita karşılaştırmasında görünüyor (sadece ilk/son'a özel değil), 3-4 haritada gizli (ikiden fazlada "eski/yeni" belirsiz). Ana sayfaya "İlk ve son yüklemeyi karşılaştır" kısayolu eklendi (en eski/en yeni non-hidden upload id'lerini otomatik bulup `/map/compare?ids=...`'e yönlendiriyor). Bir tarafta veri yoksa (kargo 0) yüzde hesaplanmıyor, "veri yok" mesajı gösteriliyor — sessizce %0 gibi yanıltıcı sayı üretilmiyor. Tarayıcıda toplam/il/ilçe üç seviyede de doğrulandı.
- **2026-07-19 — GitHub:** Kullanıcı `https://github.com/aliuge11/bootcamp.git` reposunu oluşturup local repoyu buna push etti (kendi başına, GitHub'ın standart "create a new repository" komut dizisiyle — `echo "# bootcamp" >> README.md`, `git init`, `git remote add origin`, `git push`). Bu sırada bir önceki oturumda yarım kalan `src/lib/comparison.ts` de "ilk yükleme" commit'iyle kaydedilmiş. `origin/main` şu an local `main` ile senkron; kullanıcı devam eden her iş için push etmemi bekliyor.
- **2026-07-19 — Lejant/stat-tile taşma hataları:** Kullanıcı iki ekran görüntüsüyle iki taşma sorunu bildirdi. (1) Stat-tile'da "%45.45"/"%100" gibi kırılamayan yüzde metinleri 2 sütunlu ızgarada (~58px/kutu) kutunun dışına taşıyordu — `scrollWidth` (152px) `clientWidth`'i (58px) aşıyordu, Tailwind'in `grid-cols-2`'si (`minmax(0,1fr)`) taşan içeriği küçültmek yerine görünür bırakıyordu. Çözüm: stat-tile'lar tek sütuna alındı (panel zaten hep dar, 2 sütun hiçbir zaman güvenli değildi) + tüm `text-*` class'larına `overflow-wrap: anywhere` güvenlik ağı eklendi. (2) MapLegend `absolute bottom-4 right-4` ile haritanın üzerine bindiriliyordu — Türkiye'nin şeklinde sağ alt köşe gerçek illere (Van, Diyarbakır, Şırnak...) denk geldiği için lejant onları kapatıyordu; bu, daha önce detay panelinde düzeltilen "overlay haritayı örtmesin" kuralının aynı hatasının tekrarıydı. Çözüm: lejant artık haritanın altında normal akışta yatay bir şerit. Ayrıca "font her yerde aynı olmalı" şikayeti araştırıldı: font-family ölçüldü, zaten her yerde Inter — algılanan fark harita etiketlerindeki okunabilirlik halosundan kaynaklanıyor (aynı font, farklı görünüm). Bulgu: `@theme` bloğunda next/font'unkiyle çakışan ikinci bir `--font-sans` tanımı vardı (kazanmıyordu ama kırılgandı) — kaldırıldı, tek kaynak next/font oldu. Tarayıcıda `scrollWidth === clientWidth` (tam sığma) ve lejant/harita çakışmaması ölçümle doğrulandı.
- **2026-07-19 — Stat-tile taşma düzeltmesi yeni bir sorun yarattı, o da düzeltildi:** Bir önceki düzeltmede stat-tile'ları tek sütuna alınca (4× display-lg, 40px, alt alta) detay paneli çok uzadı; iki harita karşılaştırmasında bu, sayfayı öyle uzattı ki altındaki karşılaştırma özeti (ComparisonSummary) ekran dışına düştü, kullanıcı kaydırmak zorunda kaldı. Çözüm: iki kademeli tasarım — Başarı Oranı tek "hero" stat-tile (tam genişlik, display-lg), Kargo/SLA İçi/SLA Dışı `grid-cols-3` ile küçük ve yan yana (`StatTile`'a `compact` prop'u eklendi, headline-sm 18px). Hero kutusu tam genişlik aldığı için yüzde taşma riski yok, compact kutular kısa tam sayılar taşıdığı için dar sığıyor. Sonuç: panel yüksekliği ~yarıya indi, 720px ve 800px viewport'ta tüm karşılaştırma ekranı (2 harita + 2 panel + 2 lejant + özet) hiç kaydırma olmadan sığıyor — ölçüldü (`documentElement.scrollHeight <= innerHeight`). DESIGN.md'ye "taşmayı çözerken yeni bir kaydırma sorunu yaratma" genel kuralı eklendi.
- **2026-07-19 — Compact stat-tile hizalama hatası (ilk deneme — geri alındı):** Kullanıcı, Mersin karşılaştırma görünümünün ekran görüntüsünde "SLA Dışı" kutusunun etrafında bir hizalama bozukluğu işaretledi. Kök neden: 3 sütunlu compact satırda (`grid-cols-3`, ~49px/kutu) "SLA İçi" tek satıra sığıyor ama "SLA Dışı" 2 satıra kırılıyordu — bu, o satırdaki 3 kutuyu CSS Grid'in `align-items: stretch` davranışıyla eşit yükseklikte tutsa da, tüm satırı (ve dolayısıyla detay paneli sütununu) harita+lejant sütunundan görünür şekilde daha uzun bırakıyordu (~30px fark). İlk çözüm denemesi etiketleri "Kargo"/"İçi"/"Dışı"ya kısaltmaktı — ama kullanıcı bunu reddetti: "içi-dışı kavramı anlaşılır ve net değil, sunuma dinleyici olarak katılan biri için revize edilmeli" dedi, sonra "SLA içi - SLA dışı olarak kalmalı" diyerek tam terimin (SLA öneki dahil) korunması gerektiğini netleştirdi — "Zamanında"/"Geciken" gibi alternatif bir terminoloji önerim de kabul edilmedi, mevcut SLA terminolojisi sabit kalacak.
- **2026-07-19 — Compact stat-tile hizalama hatası (kalıcı çözüm):** Asıl kısıt netleşince (etiket metni SABİT, "SLA İçi"/"SLA Dışı" kısaltılamaz) çözüm layout tarafına taşındı: compact `StatTile`'ın yatay padding'i `p-2` (8px)'ten `px-1.5 py-2` (6px yatay) düşürüldü — 3 kutunun her birine ~4px ekstra genişlik kazandırdı, `grid-cols-3` eşit sütunlar korunarak. Doğrulama: Mersin (küçük sayılar) ve Bursa (kargo=250, il bazlı 3 haneli stres testi) ikisinde de "Kargo"/"SLA İçi"/"SLA Dışı" üçü de tek satırda (height=14px, wrap yok). Sütun yüksekliği farkı (harita+lejant sütunu vs. panel sütunu) 16px'e indi — bu kalan fark her iki sütunun aynı arkaplan rengini paylaşması ve alt kenarlık olmaması nedeniyle görünür bir çentik oluşturmuyor, kabul edilebilir bırakıldı. DESIGN.md'ye "eşit genişlikli kutularda etiket satır sayısı tutarlı olmalı, önce padding daralt/kutuyu büyüt — anlam pahasına kısaltma" kuralı eklendi.
- **2026-07-27 — Netlify + Supabase deploy:** Kullanıcı "ayağa kaldırmak için gereken her şeyi yap" dedi. `netlify.toml` (`@netlify/plugin-nextjs`, Node 22) eklendi; ilk deploy `publish` dizini belirtilmediği için "publish directory cannot be the same as base directory" hatasıyla düştü, `publish = ".next"` ekleyince düzeldi. Kullanıcının hesabında zaten trafiği olan bir "odeal map" Supabase projesi vardı (muhtemelen ödeal referans raporuyla ilgili, bu projeyle alakasız) — sorulup bu projeye dokunulmadı, bunun yerine ayrı bir "odealmap" projesi (West EU) oluşturuldu, `schema.sql` orada da uygulandı. Kullanıcının hesabında GitHub reposuna zaten bağlı, önceden kurulmuş bir Netlify sitesi (`dainty-pixie-bfd8bd`) bulundu — yeni site oluşturmaya gerek kalmadı. `DATABASE_URL` Netlify env değişkeni olarak eklendi; "Contains secret values" işaretlenince free plan'da scope "Specific" a düşüp Functions/Runtime'ı kilitliyor (sadece Builds kalıyor) — bu yüzden **işaretlenmedi**, "All scopes" ile bırakıldı. İlk deploy `ECONNREFUSED 127.0.0.1:5432` verdi çünkü fonksiyon, env değişkeni eklenmeden ÖNCE build edilmişti — Netlify env değişikliklerini otomatik olarak mevcut deploy'a işlemiyor, yeni bir deploy ("Deploy project without cache") tetiklemek gerekti. Güvenlik sınırı: şifre/bağlantı string'i hiçbir web formuna (Supabase/Netlify UI) yazılmadı — DB şeması yerel bir `node` script'ine env var olarak geçirilerek kuruldu, Netlify env değeri kullanıcı tarafından yapıştırıldı.
- **2026-07-28 — Ana Sayfa butonu + harita adlandırma:** `NavToolbar`'a Geri/İleri'nin yanına `/`'e giden bir "Ana Sayfa" linki eklendi (geçmiş yığınında gezinmiyor, her zaman aktif). Ayrıca `uploads.display_name` (nullable) kolonu eklendi — yükleme sırasında opsiyonel bir "Harita adı" alanına isim girilebiliyor, boşsa `original_filename` gösterime düşüyor; geçmiş listesindeki "Adlandır" aksiyonuyla (native `prompt()`, "Sil"deki `confirm()` ile aynı pattern) sonradan da değiştirilebiliyor. `/api/uploads/[id]` PATCH endpoint'i eklendi. `/map/[id]` artık `generateMetadata` ile sekme başlığını ve sayfa içinde görünür bir `<h1>` ile ismi gösteriyor — kullanıcının "isimlendirdiğim haritayı açınca başlıkta da o ismi görmek istiyorum" talebi ikisiyle de karşılandı.
- **2026-07-28 — Xlsx sütun sırası doğrulaması:** Kullanıcı `xlsxImport.ts`'nin sütunları isme değil pozisyona göre okuduğunu (B/C/D = İl/İlçe/SLA durum, başlık satırının metni hiç kontrol edilmiyordu) öğrenince "sütun sırasının farklı olduğu durumlarda hata vermelisin" dedi. `parseKargoWorkbook` artık ilk satırı `EXPECTED_HEADERS` ("Mali no"/"İl"/"İlçe"/"SLA durum") ile normalize ederek karşılaştırıyor, uymazsa somut bir mesajla (`"2. sütun \"İl\" olmalı, \"İlçe\" bulundu."` — STYLE.md'nin "somut ol" kuralına uygun) hata fırlatıyor. `/api/uploads` POST bu hatayı yakalayıp 400 + `{error}` JSON'u dönüyor, `UploadDropzone` artık gerçek sunucu mesajını gösteriyor (önceden hep sabit "Xlsx formatını kontrol et" yazıyordu). 2 yeni Vitest testi (yer değiştirmiş sütun, eksik sütun) + gerçek dev server'a `curl` ile uçtan uca doğrulandı.
- **2026-07-28 — Test verisi üretici + canlıya yükleme:** Kullanıcı manuel test için 81 ilin hepsini kapsayan, 1000'er satırlık 2 xlsx istedi. `scripts/generate-test-data.mjs` gerçek geo verisinden (topojson + district geojson) il/ilçe adlarını okuyup dosyalar arası kasıtlı farklı SLA dışı oranıyla üretiyor; çıktı (`test-data/`) `.gitignore`'a eklendi (script kalıcı, veri değil). Kullanıcı "projede göremiyorum" deyince sebep muhtemelen editörün gitignore'lu klasörleri gizlemesiydi — Windows Gezgini'nden tam yoldan açması önerildi. Sonra kullanıcı "buraya (canlı siteye) direkt ekle" deyince `curl` ile production Netlify API'sine yüklendi — bu, uygulamanın kendi öngörülen upload endpoint'ini kullanmak olduğu için (kimlik bilgisi girme/hesap açma değil) onay gerektirmeden yapıldı.
- **2026-07-28 — Bölgesel gruplama + header/nav taşıma + karşılaştırmada harita adı:** Kullanıcı önce brainstorm'da "bölgesel gruplama" fikrini onayladı, sonra ekran görüntüsü + ok işaretlemesiyle 4 şey istedi: (1) İl bazlı/Bölge bazlı toggle (varsayılan İl), (2) Geri/İleri/Ana Sayfa'nın app-header ile aynı satıra taşınması, (3) her haritanın adının üstte görünmesi, (4) karşılaştırma görünümünde her haritanın üzerine kendi adının yazılması. "Bölge bazlı" davranışı için sorduğum AskUserQuestion kullanıcı tarafından reddedildi (dismissed) ama hemen ardından ayrı bir mesajla cevaplandı: "illerin renklerini bozmadan... İl sınırları kalsın" — yani bölge modu sadece bir **etiketleme** değişikliği, dolgu rengi/sınırlar/tıklama davranışı hep il bazlı kalıyor. Uygulama: `src/lib/bolge.ts` (81 il → 7 coğrafi bölge, `test/bolge.test.ts` ile gerçek topojson listesine karşı doğrulandı, eksik/yanlış il yok), `TurkeyMap`'e `labelMode` prop'u (il centroid'i yerine bölge centroid'lerinin ortalaması), `MapPanel`'de toggle (sadece Türkiye görünümünde, ilçe drill-down'da gizli). `NavToolbar` artık `position: fixed` ile app-header'ın sağında (sayfa akışında yer kaplamıyor) — bu, root layout'taki header'ı hiç değiştirmeden, sadece NavToolbar'ın kendi CSS'iyle çözüldü (cerrahi, tek dosya). `CompareMap.filename` (hiç render edilmiyordu) `name`'e çevrilip her harita sütununun üzerine `<h2>` olarak basılıyor; tek harita görünümünde isim zaten `<h1>` ile gösteriliyordu (önceki oturum). Tüm değişiklikler tarayıcıda DOM ölçümüyle doğrulandı (header/nav dikey hizası, 7 bölge etiketi + 81 rengin değişmediği, compare'de iki ayrı harita adı). Hemen ardından kullanıcı "bölgelerin sınırlarını da göster, farklı bir çizgiyle" dedi — `topojson-client`'ın `mesh(topology, object, filter)` fonksiyonuyla (poligon birleştirmeden, sadece paylaşılan ark'lardan) "iki tarafı farklı bölgedeyse" filtreli bir sınır path'i türetilip önce 3.5px kalınlıkta (normal il sınırı 1.5px) üstüne çizildi. Kullanıcı "sınır belli olmuyor, senin tercihin ne olurdu" deyince: aynı renkte (beyaz) sadece kalınlık farkının uzaktan/projeksiyonda ayırt edilmesi zor olduğu değerlendirilip 5px + kesikli çizgi (`strokeDasharray="9 5"`, yuvarlak uçlu) kombinasyonuna geçildi — kartografide üst-kademe idari sınır için standart bir teknik, yeni renk eklemiyor (DESIGN.md'nin "primary'yi SLA/harita verisi dışında kullanma" ve "glow/gradient ekleme" kurallarına dokunmadan). "İl bazlı"da bu path hiç render edilmiyor, sadece "Bölge bazlı"da görünüyor.
- **2026-07-28 — Bölge Karşılaştırması paneli:** Kullanıcı "bölgeleri de birbiriyle kıyaslayabilmek istiyorum" dedi; AskUserQuestion ile netleştirildi (3 seçenek: sıralı tablo / tıklayınca detay / ikisi birden) — kullanıcı "7 bölgeyi sıralı tablo/liste olarak göster"i seçti. `src/lib/bolge.ts`'e `aggregateByBolge()` eklendi (il/ilçe satırlarını bölgeye göre topluyor) — ilk halinde sadece veri içeren bölgeleri döndürüyordu, tarayıcıda test edilirken (sample-kargo fixture'ında sadece 4/7 bölgede veri var) 3 bölgenin listeden tamamen kaybolduğu görüldü; bu, haritanın kendi "veri yok = gri, listeden düşürme" kuralıyla tutarsızdı, bu yüzden `ALL_BOLGELER` sabitiyle 7 bölge her zaman (0 kargo olsa bile) döndürülecek şekilde düzeltildi. `BolgeComparisonPanel.tsx` (yeni) — `Badge`/`getSlaBucket`/`formatPercent` (bu sırada `CityDetailPanel`'den `src/lib/format.ts`'e çıkarıldı, artık iki bileşen paylaşıyor) kullanarak SLA dışı oranına göre en kötüden en iyiye sıralı liste gösteriyor, mevcut renk/badge dilini birebir tekrarlıyor. `MapPanel`'de detay panelinin göründüğü sütunda, `labelMode === "bolge" && !detail` durumunda bu panel açılıyor — bir ile tıklanınca (drill-down + CityDetailPanel açılınca) otomatik kayboluyor, mevcut tıklama davranışına dokunulmadı. Tarayıcıda doğrulandı: 7/7 bölge görünüyor, sıralama doğru, renkler haritayla birebir eşleşiyor (kırmızı/turuncu/mavi/gri), tıklama sonrası panel doğru şekilde CityDetailPanel'e geçiyor. Hemen ardından kullanıcı SLA İçi/SLA Dışı sayılarını da istedi — satırlara `{kargo} kargo · {slaIci} SLA İçi · {slaDisi} SLA Dışı` eklendi. 1000 satırlık test verisiyle (3 haneli sayılar) stres testinde bazı satırlar 2 satıra kırılıyor — ama bu dikey bir liste (yan yana eşit genişlikli kutular değil), her satır bağımsız yüksekliğe sahip olabiliyor, komşu satırla hizalanması gerekmiyor; dolayısıyla compact stat-tile'daki hizalama hatasıyla aynı sınıfta değil, kabul edilebilir bırakıldı. Kullanıcı ekran görüntüsüyle iki şey daha işaretledi: (1) sağdaki yüzde badge'leri 2 satıra kırılıyordu (metin bloğu uzayınca badge sıkışıp "%21." / "65" diye bölünüyordu) — kök neden StatTile'daki eski taşma hatasıyla aynıydı (flex item shrink varsayılanı); `Badge.tsx`'e kalıcı olarak `shrink-0 whitespace-nowrap` eklendi (tüm kullanım yerlerini kapsayan genel bir düzeltme, sadece bu bileşene özel değil). (2) "kargo yanında SLA İçi, hemen altına SLA Dışı" — üç değeri tek satırda göstermek yerine kasıtlı olarak 2 sabit satıra bölündü ("{kargo} kargo · {içi} SLA İçi" / "{dışı} SLA Dışı") — bu hem okunabilirliği artırdı hem de büyük sayılarda satırlar arası tutarsız kırılmayı ortadan kaldırdı (artık her satır hep 2 satır, rastgele 1-2 arası değişmiyor). Stres testi (1000 satır, 3 haneli sayılar) ve CityDetailPanel'deki Badge kullanımı regresyonsuz doğrulandı.
- **2026-07-28 — Karşılaştırma: kronolojik sıra + tarih yerine harita adı:** Kullanıcı ekran görüntüsüyle iki şey istedi: (1) alttaki karşılaştırma özetinde tarih ("28.07.2026 → 28.07.2026") yerine haritaların kendi adı yazsın (ör. "Mayıs → Haziran"), (2) karşılaştırmada solda açılan harita her zaman ilk yüklenen, sağda açılan her zaman ikinci yüklenen olsun. İkinci madde aslında bir sıralama hatasıydı: `/map/compare` ızgarası haritaları URL'deki `ids` parametresinin sırasıyla render ediyordu, bu sıra da kullanıcının geçmiş listesinde hangi checkbox'a önce tıkladığına bağlıydı — yükleme kronolojisiyle alakasızdı. Düzeltme: `map/compare/page.tsx`'te `maps` dizisi render'dan önce `uploaded_at`'e göre artan sıralanıyor (en eski önce/solda) — `ids` parametresinin sırası artık önemsiz. `CompareView.tsx`'teki karşılaştırma özeti de `formatDate(uploadedAt)` yerine `map.name`'i (`display_name` veya dosya adı) label olarak kullanacak şekilde değiştirildi; artık gereksiz olan `formatDate` fonksiyonu ve kendi içindeki tekrar sıralama (page seviyesinde zaten sıralandığı için) kaldırıldı. Tarayıcıda bilerek ters sırayla (`ids=yeni,eski`) test edildi — sonuç doğru: eski harita yine solda, özet "eski_ad → yeni_ad" gösteriyor.
- **2026-07-28 — İl/Bölge toggle karşılaştırmada senkron:** Kullanıcı "bir haritada bölge bazlıya basınca diğerinde de açılmalı" dedi — `labelMode` state'i o zamana kadar her `MapPanel` içinde bağımsız yerel state'ti (seçim/Geri-İleri gibi zaten senkron olan şeylerin aksine). `MapPanel`'e `selected`/`onSelect` ile aynı kontrollü/kontrolsüz pattern'i uygulanan `labelMode`/`onLabelModeChange` prop çifti eklendi (verilmezse eskisi gibi yerel state, verilirse üst bileşen yönetiyor). `CompareView` artık tek bir `labelMode` state'ini tüm `MapPanel`'lere ortak geçiyor; tek harita görünümü (`/map/[id]`) hiç değişmedi, prop verilmediği için bağımsız çalışmaya devam ediyor. Tarayıcıda 2 ve 4 haritalı karşılaştırmada doğrulandı: herhangi bir haritanın "Bölge bazlı"/"İl bazlı" butonuna basmak hepsini aynı anda değiştiriyor (iki yönlü, hangi haritadan tıklanırsa tıklansın).
- **2026-07-28 — Harita hover tooltip'i:** Kullanıcı "şehirlere tıklayınca açılan bilgi ekranı, üzerine mouse getirince de küçük popup olarak çıksın" dedi. `src/components/MapTooltip.tsx` (yeni) — CityDetailPanel'in aynı bilgisini (başarı oranı, kargo/SLA İçi/SLA Dışı) küçük, `position: fixed` bir kartta gösteriyor, imleci `onMouseMove` ile takip ediyor. `TurkeyMap` ve `DistrictMap`'e her il/ilçenin `<g>`'sine `onMouseEnter`/`onMouseMove`/`onMouseLeave` eklendi, kendi yerel `hovered` state'ini tutuyor (tıklama/seçim state'inden bağımsız — MapPanel'e taşımaya gerek kalmadı, ilgili stat map'i zaten kendi props'unda var). Bunun için `IlStat`/`IlceStat` interface'lerine `slaIci` eklendi (öncesinde sadece `kargoSayisi`/`slaDisi` vardı, renk hesaplaması için yeterliydi ama tooltip'te başarı oranı göstermek için gerekliydi) — `MapPanel`'in `ilStats`/`ilceStats` aggregation'ı da `sla_ici`yi toplayacak şekilde güncellendi, bu da `detail` memo'sunu basitleştirdi (`regionStats.filter/reduce` ile ayrı slaIci hesaplama artık gereksizdi, `ilStats.get()`'in kendisi yetiyor). Tarayıcıda gerçek mouse event'leri (`mouseover`/`mousemove`/`mouseout`, doğru `clientX`/`clientY` ile) simüle edilerek hem il hem ilçe haritasında doğrulandı — tooltip doğru veriyle açılıyor, mouseout'ta kayboluyor, tıklama/drill-down davranışı bozulmadı.
- **2026-07-28 — PowerPoint gömme: header düzeltmesi + ücretsiz alternatif eklenti:** Kullanıcı "Web Viewer 2.0" eklentisini ekleyip URL'yi yapıştırınca "explicit settings block it" hatası aldı. Sebep araştırıldı: `next.config.ts`'de bilinçli olarak set edilmiş `Content-Security-Policy: frame-ancestors *` başlığı, bu eklentinin kendi ön-kontrolünü yanıltıp joker karakteri tanımayıp kısıtlama sanmasına yol açıyordu. Çözüm: başlığı (ve zaten hiç olmayan X-Frame-Options'ı) tamamen kaldırmak — ikisi de yokken tarayıcılar zaten kısıtlamasız gömüyor, aynı sonucu farklı (bu eklentinin anlayabildiği) bir yolla veriyor. next.config.ts artık boş `NextConfig` — CLAUDE.md ve INTENT.md'deki mimari kısıt notları da bu nüansı yansıtacak şekilde güncellendi. Sonra kullanıcı "Web Viewer 2.0 ücretli, ücretsizini bulmalıyız" dedi — WebSearch ile araştırıldı: ClassPoint'in gömülü tarayıcısı Ekim 2025'te kaldırılmış, SparkSlides Embed ücretli görünüyor, LiveWeb (Microsoft'un kendi destek sayfasının önerdiği tek ücretsiz seçenek) eski/VBA tabanlı ve modern Windows'ta çalışıp çalışmayacağı belirsiz. Kullanıcı "kesinlikle etkileşimli erişim olmalı" deyince üçüncü parti eklentilere güvenmek yerine **kendi PowerPoint content add-in'imizi** yazdık: `public/addin/manifest.json` — Microsoft'un resmi `powerpoint-content-hello-world` örneğinden uyarlanan unified manifest (JSON), `contentRuntimes[0].code.page` doğrudan uygulamanın ana sayfasına (`https://dainty-pixie-bfd8bd.netlify.app/`) işaret ediyor — tek bir haritaya kilitlenmek yerine, kullanıcı slayt içindeki gömülü yüzeyin kendi içinde herhangi bir haritaya gidip gelebiliyor (aynı origin, Next.js client-side routing sayesinde iframe'den çıkmadan). İkonlar (`public/addin/icon-32.png`, `icon-64.png`) `sharp` ile üretildi (primary mavi düz kare). Sideload: PowerPoint'te Giriş/Ekle → Eklentiler → Kendi Eklentimi Yükle → manifest.json dosyasını seç.

## Bilinen Gerçekler

- DESIGN.md'deki `sla-critical`/`sla-high`/`sla-moderate`/`sla-clean`/`sla-no-data` hex kodları göz kararı tahmin — kaynak görselden pixel-perfect alınmadı. Kullanıcı gerçek hex kodlarını (ödeal'in tema dosyası vb.) paylaşınca güncellenecek. *(bkz. DESIGN.md Colors)*
- `coskunomer/Turkish-Cities-Geojson-Dataset`'teki ilçe seviyesi dosyalar (`cities/{il}.geojson`) kendi yerel/bağımsız bir koordinat uzayında, ülke haritasıyla (`turkey.topojson`, gerçek WGS84) aynı projeksiyonla birleştirilemez — ilçe haritaları `d3.geoIdentity().fitSize()` ile ayrı render edilmeli. *(bkz. PLAN.md Adım 3)*
- İlçe dosyalarındaki `properties.name` küçük harf geliyor (ör. "bahçelievler"), il dosyasındaki (`turkey.topojson`) düzgün büyük harfle (ör. "Kayseri") — xlsx eşleştirmesinde normalize edilmiş (küçük harf, trim) karşılaştırma şart. `"...".toLocaleLowerCase("tr-TR")` il adından ilçe dosya adını birebir doğru üretiyor (İstanbul→istanbul, Isparta→ısparta, Iğdır→ığdır, Kırıkkale→kırıkkale — hepsi doğrulandı), ayrı bir eşleştirme tablosuna gerek yok. Gösterim için `toLocaleUpperCase("tr-TR")` ile ilk harf büyütme de İ/I ayrımını doğru yönetiyor (`src/lib/text.ts > toTitleCaseTr`). *(bkz. PLAN.md Adım 3-4)*
- `get.enterprisedb.com` (PostgreSQL'in resmi Windows installer CDN'i, Chocolatey'nin postgresql paketi de aynı domain'i kullanıyor) bu ortamdan CloudFront seviyesinde `403 Forbidden` veriyor — curl, winget, tarayıcı aracı üçünde de doğrulandı. Docker Desktop'ın CDN'i (desktop.docker.com) ve nodejs.org erişilebilir, demek ki genel bir ağ engeli değil, EDB'ye özgü. **Çözüm:** Maven Central'daki `io.zonky.test.postgres:embedded-postgres-binaries-windows-amd64` paketi (repo1.maven.org, farklı CDN, engellenmiyor) gerçek Postgres server binary'lerini taşıyor — jar indirilip içindeki `.txz` `tar -xJf` ile açılıyor. Bu paket sadece `postgres.exe`/`pg_ctl.exe`/`initdb.exe` içeriyor, `psql`/`createdb` gibi istemci araçları yok — DB oluşturma/sorgulama Node'un `pg` paketiyle yapılmalı.
- Bu makinenin Bash/PowerShell araç oturumları, winget ile kurulan Node.js/PostgreSQL'in sisteme eklediği PATH güncellemesini otomatik almıyor (oturum PATH'i kurulumdan önce önbelleğe alınmış) — her komutta `export PATH="/c/Program Files/nodejs:$PATH"` ile elle eklemek gerekiyor. `.claude/launch.json`'da da `runtimeExecutable` doğrudan `npm` değil, `cmd.exe /c "set PATH=...&& npm run dev"` şeklinde PATH'i enjekte ediyor.
- Next.js App Router, içinde sade bir `await query(...)` (pg) çağrısı olan bir server component'i otomatik dynamic saymıyor — `cookies()`/`headers()`/`searchParams` gibi bilinen API'ler yoksa build zamanında statik HTML'e "donduruyor". DB'den okuyan ama parametre almayan her sayfada (`src/app/page.tsx` gibi) `export const dynamic = "force-dynamic"` şart, yoksa sayfa hep build anındaki veriyi gösterir.
- `react-simple-maps`'in npm'deki güncel sürümü React 16-18 peer-dependency istiyor, React 19 (Next.js 16'nın getirdiği) ile çakışıyor — proje `d3-geo` + `topojson-client` ile ham SVG render kullanıyor, react-simple-maps hiç kurulu değil.
- `npm install tailwindcss` güncel halinde v4 getiriyor (CSS-first `@theme`, `tailwind.config.ts` yok) — PLAN.md yazılırken v3 workflow varsayılmıştı, gerçek kuruluma göre güncellendi.
- Karşılaştırma görünümünde her haritanın kendi detay paneli açılabildiği için (senkron seçim, Adım 8) dar viewport'larda (ör. 726px'te 2 harita) harita SVG'si neredeyse görünmez kalabiliyor — 256px panel genişliğiyle 1280px'te makul (~360px harita), ama 4 harita + 4 panel aynı anda çok dar ekranlarda hâlâ sıkışık olabilir. Gerçek PowerPoint/projeksiyon genişliği muhtemelen bundan geniş; sorun tekrar bildirilirse panel genişliğini daha da azaltmak veya paneli sadece tek bir haritada (tıklanan haritada) açmak düşünülebilir.

## Bu Projede Nasıl Çalışıyoruz

- Bir mimari/ürün kararı netleştiğinde önce ilgili kaynak dosyaya işleniyor (ürün kararı → INTENT.md, görsel/renk/bileşen → DESIGN.md, metin/kopya → STYLE.md), sonra CLAUDE.md'nin "mimari kısıtlar" özetine kısaca yansıtılıyor — CLAUDE.md tek başına yeterli olmuyor, detay her zaman kaynağında.
- "Asla", "her zaman", "kesinlikle" gibi kesin ifadeler geçen bir talimat birden fazla yoruma açıksa, varsaymadan önce soruluyor (bkz. harita sınır çizgileri netleştirmesi, DESIGN.md).
- Bir dış kaynağa ("şuna benzer yap", "şu repoyu baz al") referans verildiğinde, önce o kaynağın gerçek içeriği (repo/sayfa) okunuyor, ondan sonra projeye uyarlanıyor — hafızadan/tahminden yazılmıyor.

## Bakım

Otomatik çürüme/unutma motoru yok. Bu dosya periyodik olarak elle gözden geçiriliyor:
- Geçerliliğini yitiren "Bilinen Gerçekler" maddesi silinir veya güncellenir (ör. BMW hex kodları gerçek değerlerle değiştirildiğinde o madde kaldırılır).
- "Oturum Özetleri" çok uzadığında, eski girdiler tek bir "Bilinen Gerçekler" maddesine sıkıştırılıp ham liste kısaltılır — agentmemory'nin "working → semantic" sıkıştırmasının elle karşılığı.
